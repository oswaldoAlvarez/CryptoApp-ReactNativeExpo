import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    paddingHorizontal: 10,
    paddingTop: 10,
    paddingBottom: 4,
  },
  input: {
    height: 40,
    borderRadius: 10,
    paddingHorizontal: 10,
    marginBottom: 20,
    fontSize: 16,
    borderWidth: 1,
  },
  emptyTitle: {
    marginTop: 40,
  },
});
