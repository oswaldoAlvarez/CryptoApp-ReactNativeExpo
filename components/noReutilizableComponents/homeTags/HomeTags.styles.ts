import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tagContainer: {
    flexDirection: "row",
    marginBottom: 12,
  },
  tag: {
    marginRight: 8,
  },
  icon: {
    marginLeft: 4,
  },
  title: {
    fontSize: 14,
  },
  labelTagContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
});
